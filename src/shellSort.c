#include "shellSort.h"
#include <stdlib.h>
#include <stdio.h>


int *shellSort(int arr[], int numOfElem){
	int firstValue, secondValue, gap, num, i;
	i=sizeof(arr);
	
	
	int a = sizeof(int);
	
	printf("b%db", numOfElem);
	
	
	return arr;
}